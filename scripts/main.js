'use strict';

window.addEventListener('load', function () {
    var buttonView = new ButtonView();

    displayBlock();
    displayCounter();
    buttonView.displayButton();
   // buttonView.addEvent();
}, false);